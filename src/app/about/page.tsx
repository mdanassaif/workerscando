import AboutClient from './AboutClient';

export const metadata = {
  title: 'About - WorkersCanDo',
  description: 'Learn about WorkersCanDo and the 100 tools challenge.',
};

export default function AboutPage() {
  return <AboutClient />;
}
